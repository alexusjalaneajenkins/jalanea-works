{
  "name": "@apply-copilot/extension-chrome",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension for Apply Co-Pilot Turbo Mode",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf src/**/*.js src/**/*.js.map"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.3.0"
  }
}
